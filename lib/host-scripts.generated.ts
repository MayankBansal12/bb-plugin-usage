// Generated by npm run generate:collectors. Edit the original TypeScript functions.
// Checked in so BB's direct Git/path builds do not need a prebuild hook.

export const hostJsonCollectorSource = "async function hostJsonCollector(encodedInput, dependencies) {\n    const { buffer, fs, path, crypto, readline, zlib } = dependencies;\n    const scanBegin = \"__BB_USAGE_SCAN_BEGIN__\";\n    const scanEnd = \"__BB_USAGE_SCAN_END__\";\n    const input = JSON.parse(buffer.from(encodedInput, \"base64\").toString(\"utf8\"));\n    const cacheVersion = input.agentId === \"dsh\" || input.agentId === \"codex\" ? 6 : 5;\n    const allowedAgents = new Set([\"codex\", \"claude\", \"dsh\", \"fx\", \"grok\", \"pi\", \"prime\", \"antigravity\", \"thaura\"]);\n    if (!allowedAgents.has(input.agentId))\n        throw new Error(\"Unsupported usage agent.\");\n    if (!/^\\d{4}-\\d{2}-\\d{2}$/.test(input.sinceDay))\n        throw new Error(\"Invalid usage history boundary.\");\n    const accountRoot = input.agentId === \"codex\" && typeof input.accountRoot === \"string\" && input.accountRoot.trim()\n        ? input.accountRoot.replace(/\\/+$/, \"\")\n        : null;\n    const failures = [];\n    let discoveryFailed = false;\n    const cutoffMs = Date.parse(`${input.sinceDay}T00:00:00Z`);\n    const canDecompressZstd = typeof zlib.zstdDecompressSync === \"function\";\n    const accountByPath = new Map();\n    function object(value) {\n        return value !== null && typeof value === \"object\" && !Array.isArray(value) ? value : null;\n    }\n    function finite(value) {\n        return typeof value === \"number\" && Number.isFinite(value)\n            ? value\n            : typeof value === \"string\" && value.trim() && Number.isFinite(Number(value)) ? Number(value) : null;\n    }\n    function count(value) {\n        return Math.max(0, Math.round(finite(value) ?? 0));\n    }\n    function text(value, fallback) {\n        return typeof value === \"string\" && value.trim() ? value : fallback;\n    }\n    function projectName(value) {\n        if (typeof value !== \"string\" || !value.trim())\n            return \"Unknown\";\n        const normalized = value.replace(/\\\\/g, \"/\").replace(/\\/+$/, \"\");\n        const segment = normalized.slice(normalized.lastIndexOf(\"/\") + 1);\n        return segment.trim() ? segment.trim().slice(0, 80) : \"Unknown\";\n    }\n    function day(value) {\n        if ((typeof value !== \"string\" && typeof value !== \"number\") || !String(value).trim())\n            return null;\n        const timestamp = new Date(value).getTime();\n        if (!Number.isFinite(timestamp))\n            return null;\n        const parsed = new Date(timestamp);\n        const result = `${parsed.getFullYear()}-${String(parsed.getMonth() + 1).padStart(2, \"0\")}-${String(parsed.getDate()).padStart(2, \"0\")}`;\n        return result >= input.sinceDay ? result : null;\n    }\n    function validRow(value) {\n        const row = object(value);\n        return Boolean(row\n            && typeof row.day === \"string\"\n            && typeof row.modelProviderId === \"string\"\n            && typeof row.model === \"string\"\n            && typeof row.project === \"string\"\n            && (row.account === undefined || typeof row.account === \"string\")\n            && (row.loggedCostUsd === null || finite(row.loggedCostUsd) !== null)\n            && finite(row.uncachedInputTokens) !== null\n            && finite(row.cachedInputTokens) !== null\n            && finite(row.cacheWriteTokens) !== null\n            && finite(row.outputTokens) !== null\n            && (row.eventKey === undefined || typeof row.eventKey === \"string\"));\n    }\n    function validCacheEntry(value) {\n        const entry = object(value);\n        return Boolean(entry && typeof entry.signature === \"string\"\n            && Array.isArray(entry.rows) && entry.rows.every(validRow));\n    }\n    function add(target, raw) {\n        if (raw.day < input.sinceDay)\n            return;\n        const row = {\n            day: raw.day,\n            modelProviderId: text(raw.modelProviderId, \"unknown\"),\n            model: text(raw.model, \"unknown\"),\n            project: text(raw.project, \"Unknown\"),\n            loggedCostUsd: finite(raw.loggedCostUsd),\n            uncachedInputTokens: count(raw.uncachedInputTokens),\n            cachedInputTokens: count(raw.cachedInputTokens),\n            cacheWriteTokens: count(raw.cacheWriteTokens),\n            outputTokens: count(raw.outputTokens),\n        };\n        const account = text(raw.account, \"\");\n        if (account)\n            row.account = account;\n        const keyed = new Set([\"pi\", \"prime\", \"thaura\"]).has(input.agentId);\n        const key = JSON.stringify([row.day, row.modelProviderId, row.model, row.project, row.account ?? null,\n            keyed ? (row.loggedCostUsd !== null && row.loggedCostUsd > 0 ? \"logged\" : \"estimate\") : \"all\"]);\n        const prior = target.get(key);\n        if (!prior) {\n            target.set(key, row);\n            return;\n        }\n        prior.uncachedInputTokens += row.uncachedInputTokens;\n        prior.cachedInputTokens += row.cachedInputTokens;\n        prior.cacheWriteTokens += row.cacheWriteTokens;\n        prior.outputTokens += row.outputTokens;\n        if (row.loggedCostUsd !== null)\n            prior.loggedCostUsd = (prior.loggedCostUsd ?? 0) + row.loggedCostUsd;\n    }\n    function mergeEvent(target, raw) {\n        if (!raw.eventKey)\n            return;\n        const prior = target.get(raw.eventKey);\n        if (!prior) {\n            target.set(raw.eventKey, { ...raw });\n            return;\n        }\n        prior.uncachedInputTokens = Math.max(prior.uncachedInputTokens, raw.uncachedInputTokens);\n        prior.cachedInputTokens = Math.max(prior.cachedInputTokens, raw.cachedInputTokens);\n        prior.cacheWriteTokens = Math.max(prior.cacheWriteTokens, raw.cacheWriteTokens);\n        prior.outputTokens = Math.max(prior.outputTokens, raw.outputTokens);\n        if (raw.loggedCostUsd !== null)\n            prior.loggedCostUsd = Math.max(prior.loggedCostUsd ?? 0, raw.loggedCostUsd);\n    }\n    function matches(filePath) {\n        const name = path.basename(filePath);\n        if (input.agentId === \"codex\")\n            return name.startsWith(\"rollout-\") && name.endsWith(\".jsonl\");\n        if (input.agentId === \"dsh\")\n            return name === \"session.v3.jsonl.zstd\";\n        if (input.agentId === \"fx\" || input.agentId === \"antigravity\" || input.agentId === \"thaura\")\n            return name === \"usage.jsonl\";\n        if (input.agentId === \"grok\")\n            return name === \"unified.jsonl\";\n        return name.endsWith(\".jsonl\");\n    }\n    async function walk(directory, files) {\n        let entries;\n        try {\n            entries = await fs.promises.readdir(directory, { withFileTypes: true });\n        }\n        catch (error) {\n            const code = error.code;\n            if (code === \"ENOTDIR\") {\n                try {\n                    const stat = await fs.promises.stat(directory);\n                    if (stat.isFile() && matches(directory)) {\n                        files.push(directory);\n                        return;\n                    }\n                }\n                catch {\n                }\n            }\n            if (code !== \"ENOENT\") {\n                discoveryFailed = true;\n                failures.push(\"A usage directory could not be read.\");\n            }\n            return;\n        }\n        for (const entry of entries) {\n            const entryPath = path.join(directory, entry.name);\n            if (entry.isDirectory())\n                await walk(entryPath, files);\n            else if (entry.isFile() && matches(entryPath))\n                files.push(entryPath);\n        }\n    }\n    function zstdFrames(source) {\n        const frames = [];\n        let offset = 0;\n        while (offset < source.length) {\n            const start = offset;\n            if (source.length - offset < 4)\n                return { frames, tornStart: start };\n            if (source.readUInt32LE(offset) !== 0xfd2fb528)\n                throw new Error(\"Invalid Zstandard frame magic.\");\n            offset += 4;\n            if (offset === source.length)\n                return { frames, tornStart: start };\n            const descriptor = source.readUInt8(offset);\n            offset += 1;\n            if ((descriptor & 24) !== 0)\n                throw new Error(\"Invalid Zstandard frame header.\");\n            const singleSegment = (descriptor & 32) !== 0;\n            const contentSizeFlag = descriptor >>> 6;\n            const dictionaryBytes = (descriptor & 3) === 3 ? 4 : descriptor & 3;\n            const contentSizeBytes = contentSizeFlag === 0 ? (singleSegment ? 1 : 0) : 1 << contentSizeFlag;\n            const headerBytes = (singleSegment ? 0 : 1) + dictionaryBytes + contentSizeBytes;\n            if (source.length - offset < headerBytes)\n                return { frames, tornStart: start };\n            offset += headerBytes;\n            for (;;) {\n                if (source.length - offset < 3)\n                    return { frames, tornStart: start };\n                const blockHeader = source.readUIntLE(offset, 3);\n                offset += 3;\n                const blockType = (blockHeader >>> 1) & 3;\n                if (blockType === 3)\n                    throw new Error(\"Invalid Zstandard block type.\");\n                const payloadBytes = blockType === 1 ? 1 : blockHeader >>> 3;\n                if (source.length - offset < payloadBytes)\n                    return { frames, tornStart: start };\n                offset += payloadBytes;\n                if ((blockHeader & 1) !== 0)\n                    break;\n            }\n            if ((descriptor & 4) !== 0) {\n                if (source.length - offset < 4)\n                    return { frames, tornStart: start };\n                offset += 4;\n            }\n            frames.push({ start, end: offset });\n        }\n        return { frames };\n    }\n    async function* zstdLines(filePath) {\n        if (!canDecompressZstd)\n            throw new Error(\"This Node.js cannot decompress Zstandard usage logs.\");\n        const source = await fs.promises.readFile(filePath);\n        const { frames, tornStart } = zstdFrames(source);\n        for (const frame of frames) {\n            yield* zlib.zstdDecompressSync(source.subarray(frame.start, frame.end)).toString(\"utf8\").split(\"\\n\");\n        }\n        if (tornStart !== undefined) {\n            try {\n                yield* zlib.zstdDecompressSync(source.subarray(tornStart), { finishFlush: zlib.constants.ZSTD_e_flush }).toString(\"utf8\").split(\"\\n\");\n            }\n            catch { }\n        }\n    }\n    function lastStreamUsage(stream) {\n        if (!Array.isArray(stream))\n            return null;\n        let usage = null;\n        for (const entry of stream) {\n            const chunk = object(object(entry)?.chunk);\n            if (chunk?.type !== \"usage\")\n                continue;\n            const candidate = object(chunk.usage);\n            if (candidate)\n                usage = candidate;\n        }\n        return usage;\n    }\n    async function parseFile(filePath) {\n        const rows = new Map();\n        const events = new Map();\n        const fileAccount = accountByPath.get(filePath);\n        let codexModel = \"codex-unknown\";\n        let codexSessionId = path.basename(filePath);\n        let sessionProject = \"Unknown\";\n        let dshProvider = \"unknown\";\n        let dshModel = \"unknown\";\n        let dshSeeded = false;\n        let dshEndSeedSeq = -1;\n        const dshSettlements = [];\n        let dshLastSettlement;\n        const lines = filePath.endsWith(\".zstd\")\n            ? zstdLines(filePath)\n            : readline.createInterface({ input: fs.createReadStream(filePath, { encoding: \"utf8\", highWaterMark: 1024 * 1024 }), crlfDelay: Infinity });\n        for await (const line of lines) {\n            if (!line.trim())\n                continue;\n            let raw;\n            try {\n                raw = JSON.parse(line);\n            }\n            catch {\n                continue;\n            }\n            const value = object(raw);\n            if (!value)\n                continue;\n            if (input.agentId === \"codex\") {\n                const payload = object(value.payload);\n                if (value.type === \"session_meta\" && payload)\n                    codexSessionId = text(payload.id, codexSessionId);\n                if ((value.type === \"turn_context\" || value.type === \"session_meta\") && payload) {\n                    codexModel = text(payload.model, codexModel);\n                    if (typeof payload.cwd === \"string\")\n                        sessionProject = projectName(payload.cwd);\n                }\n                if (value.type !== \"event_msg\" || payload?.type !== \"token_count\")\n                    continue;\n                const usage = object(object(payload.info)?.last_token_usage);\n                const usageDay = day(value.timestamp);\n                if (!usage || !usageDay)\n                    continue;\n                const inputTokens = count(usage.input_tokens);\n                const cached = Math.min(inputTokens, count(usage.cached_input_tokens));\n                add(rows, {\n                    day: usageDay, modelProviderId: \"openai\", model: codexModel, project: sessionProject, loggedCostUsd: null,\n                    account: fileAccount,\n                    uncachedInputTokens: inputTokens - cached, cachedInputTokens: cached,\n                    cacheWriteTokens: count(usage.cache_write_input_tokens), outputTokens: count(usage.output_tokens),\n                });\n                continue;\n            }\n            if (input.agentId === \"claude\") {\n                if (value.type !== \"assistant\")\n                    continue;\n                const message = object(value.message);\n                const usage = object(message?.usage);\n                const usageDay = day(value.timestamp);\n                if (!message || !usage || !usageDay)\n                    continue;\n                const model = text(message.model, \"claude-unknown\");\n                const uncached = count(usage.input_tokens);\n                const cached = count(usage.cache_read_input_tokens);\n                const writes = count(usage.cache_creation_input_tokens);\n                const output = count(usage.output_tokens);\n                if (model === \"<synthetic>\" && uncached + cached + writes + output === 0)\n                    continue;\n                const rawIdentity = typeof message.id === \"string\" && message.id\n                    ? `message:${message.id}`\n                    : typeof value.requestId === \"string\" && value.requestId ? `request:${value.requestId}` : null;\n                if (typeof value.cwd === \"string\")\n                    sessionProject = projectName(value.cwd);\n                const row = {\n                    day: usageDay, modelProviderId: \"anthropic\", model, project: sessionProject, loggedCostUsd: null,\n                    uncachedInputTokens: uncached, cachedInputTokens: cached, cacheWriteTokens: writes, outputTokens: output,\n                    eventKey: rawIdentity ? crypto.createHash(\"sha256\").update(rawIdentity).digest(\"hex\") : undefined,\n                };\n                if (row.eventKey)\n                    mergeEvent(events, row);\n                else\n                    add(rows, row);\n                continue;\n            }\n            if (input.agentId === \"grok\") {\n                if (value.msg !== \"shell.turn.inference_done\")\n                    continue;\n                const usage = object(value.ctx);\n                const usageDay = day(value.ts);\n                if (!usage || usage.prompt_tokens === undefined || !usageDay)\n                    continue;\n                const prompt = count(usage.prompt_tokens);\n                const cached = Math.min(prompt, count(usage.cached_prompt_tokens));\n                if (typeof value.cwd === \"string\")\n                    sessionProject = projectName(value.cwd);\n                add(rows, {\n                    day: usageDay, modelProviderId: \"xai\", model: text(usage.model, \"grok-build-0.1\"), project: sessionProject, loggedCostUsd: null,\n                    uncachedInputTokens: prompt - cached, cachedInputTokens: cached, cacheWriteTokens: 0,\n                    outputTokens: count(usage.completion_tokens) + count(usage.reasoning_tokens),\n                });\n                continue;\n            }\n            if (input.agentId === \"fx\") {\n                if (value.kind !== \"generation\")\n                    continue;\n                const fact = object(value.fact);\n                const usageDay = day(fact?.created_at_ms);\n                if (!fact || !usageDay)\n                    continue;\n                const model = text(fact.model, \"unknown\");\n                const separator = model.indexOf(\"/\");\n                const inputTokens = count(fact.input_tokens);\n                const cached = Math.min(inputTokens, count(fact.cache_read_tokens));\n                const cacheWrite = Math.min(inputTokens - cached, count(fact.cache_write_tokens));\n                add(rows, {\n                    day: usageDay,\n                    modelProviderId: separator > 0 ? model.slice(0, separator) : \"unknown\",\n                    model,\n                    project: projectName(fact.cwd ?? fact.workspace ?? value.cwd),\n                    loggedCostUsd: finite(fact.total_cost),\n                    uncachedInputTokens: inputTokens - cached - cacheWrite,\n                    cachedInputTokens: cached,\n                    cacheWriteTokens: cacheWrite,\n                    outputTokens: count(fact.output_tokens),\n                });\n                continue;\n            }\n            if (input.agentId === \"antigravity\") {\n                if (value.kind !== \"generation\")\n                    continue;\n                const fact = object(value.fact);\n                const usageDay = day(fact?.created_at_ms);\n                if (!fact || !usageDay)\n                    continue;\n                const inputTokens = count(fact.input_tokens);\n                const cached = Math.min(inputTokens, count(fact.cache_read_tokens));\n                add(rows, {\n                    day: usageDay,\n                    modelProviderId: text(fact.provider, \"google\"),\n                    model: text(fact.model, \"unknown\"),\n                    project: projectName(fact.cwd ?? fact.workspace ?? value.cwd),\n                    loggedCostUsd: finite(fact.total_cost),\n                    uncachedInputTokens: inputTokens - cached,\n                    cachedInputTokens: cached,\n                    cacheWriteTokens: 0,\n                    outputTokens: count(fact.output_tokens),\n                });\n                continue;\n            }\n            if (input.agentId === \"thaura\") {\n                if (value.kind !== \"generation\")\n                    continue;\n                const fact = object(value.fact);\n                const usageDay = day(fact?.created_at_ms);\n                if (!fact || !usageDay)\n                    continue;\n                const model = text(fact.model, \"thaura\");\n                const inputTokens = count(fact.input_tokens);\n                const cached = Math.min(inputTokens, count(fact.cache_read_tokens));\n                add(rows, {\n                    day: usageDay,\n                    modelProviderId: \"thaura\",\n                    model,\n                    project: projectName(fact.cwd ?? value.cwd),\n                    loggedCostUsd: finite(fact.total_cost),\n                    uncachedInputTokens: inputTokens - cached,\n                    cachedInputTokens: cached,\n                    cacheWriteTokens: 0,\n                    outputTokens: count(fact.output_tokens),\n                });\n                continue;\n            }\n            if (input.agentId === \"pi\" || input.agentId === \"prime\") {\n                const directory = value.cwd ?? value.directory ?? object(value.session)?.cwd;\n                if (typeof directory === \"string\")\n                    sessionProject = projectName(directory);\n                if (value.type !== \"message\")\n                    continue;\n                const message = object(value.message);\n                const usage = object(message?.usage);\n                const usageDay = day(value.timestamp ?? message?.timestamp);\n                if (!message || message.role !== \"assistant\" || !usage || !usageDay)\n                    continue;\n                const loggedCostUsd = finite(object(usage.cost)?.total);\n                const hasTokens = count(usage.input) + count(usage.cacheRead) + count(usage.cacheWrite) + count(usage.output) > 0;\n                if (!hasTokens && !(loggedCostUsd !== null && loggedCostUsd > 0))\n                    continue;\n                add(rows, {\n                    day: usageDay,\n                    modelProviderId: text(message.provider, \"unknown\"),\n                    model: text(message.responseModel, text(message.model, \"unknown\")),\n                    project: sessionProject,\n                    loggedCostUsd,\n                    uncachedInputTokens: count(usage.input), cachedInputTokens: count(usage.cacheRead),\n                    cacheWriteTokens: count(usage.cacheWrite), outputTokens: count(usage.output),\n                });\n            }\n            if (input.agentId === \"dsh\") {\n                if (value.type === \"session\") {\n                    dshSeeded = value.isSeeded === true;\n                    if (typeof value.cwd === \"string\")\n                        sessionProject = projectName(value.cwd);\n                    continue;\n                }\n                if (value.type === \"session/end-seed\") {\n                    if (object(value.data)?.inherited === true && typeof value.seq === \"number\") {\n                        dshEndSeedSeq = Math.max(dshEndSeedSeq, value.seq);\n                    }\n                    continue;\n                }\n                const data = object(value.data);\n                if (value.type === \"llm/retry-started\") {\n                    if (dshLastSettlement?.turn === data?.turn && dshLastSettlement?.step === data?.step) {\n                        dshLastSettlement = undefined;\n                    }\n                    continue;\n                }\n                if (value.type === \"request/context\" && data) {\n                    dshProvider = text(data.provider, dshProvider);\n                    dshModel = text(data.model, dshModel);\n                    continue;\n                }\n                if (value.type !== \"assistant/message\" && value.type !== \"assistant/attempt\")\n                    continue;\n                const usage = object(data?.usage) ?? lastStreamUsage(data?.stream);\n                const usageDay = day(value.time);\n                if (!usage || !usageDay)\n                    continue;\n                const inputTokens = count(usage.inputTokens);\n                const cached = count(usage.cacheReadTokens);\n                const writes = count(usage.cacheWriteTokens);\n                const output = count(usage.outputTokens);\n                const source = object(object(data?.message)?.source);\n                const replay = object(object(source?.replayState)?.response);\n                const settlement = {\n                    seq: count(value.seq),\n                    turn: count(data?.turn),\n                    step: count(data?.step),\n                    row: {\n                        day: usageDay,\n                        modelProviderId: text(source?.provider ?? replay?.provider, dshProvider),\n                        model: text(replay?.responseModel ?? source?.model ?? replay?.model, dshModel),\n                        project: sessionProject,\n                        loggedCostUsd: null,\n                        uncachedInputTokens: inputTokens, cachedInputTokens: cached,\n                        cacheWriteTokens: writes, outputTokens: output,\n                    },\n                };\n                if (dshLastSettlement?.turn === settlement.turn && dshLastSettlement.step === settlement.step) {\n                    Object.assign(dshLastSettlement, settlement);\n                }\n                else {\n                    dshSettlements.push(settlement);\n                    dshLastSettlement = settlement;\n                }\n                continue;\n            }\n        }\n        if (dshSeeded && dshEndSeedSeq < 0)\n            throw new Error(\"A seeded usage log is missing its inherited boundary.\");\n        for (const settlement of dshSettlements) {\n            if (dshSeeded && settlement.seq <= dshEndSeedSeq)\n                continue;\n            const row = settlement.row;\n            if (row.uncachedInputTokens + row.cachedInputTokens + row.cacheWriteTokens + row.outputTokens > 0)\n                add(rows, row);\n        }\n        if (input.agentId === \"codex\") {\n            return [...rows.values()].map((row) => ({\n                ...row,\n                eventKey: crypto.createHash(\"sha256\").update(JSON.stringify([\n                    \"codex\", codexSessionId, row.account ?? null, row.day, row.modelProviderId, row.model, row.project,\n                ])).digest(\"hex\"),\n            }));\n        }\n        return input.agentId === \"claude\" ? [...events.values(), ...rows.values()] : [...rows.values()];\n    }\n    let cache = { version: cacheVersion, agentId: input.agentId, files: {} };\n    try {\n        const parsed = object(JSON.parse(await fs.promises.readFile(input.cachePath, \"utf8\")));\n        const parsedFiles = object(parsed?.files);\n        if (parsed?.version === cacheVersion && parsed.agentId === input.agentId && parsedFiles) {\n            const files = {};\n            for (const [sourceId, entry] of Object.entries(parsedFiles)) {\n                if (validCacheEntry(entry))\n                    files[sourceId] = entry;\n            }\n            cache = { version: cacheVersion, agentId: input.agentId, files };\n        }\n    }\n    catch (error) {\n        if (error.code !== \"ENOENT\")\n            failures.push(\"The prior metadata cache could not be read.\");\n    }\n    const discovered = [];\n    for (const root of [...new Set(input.roots)])\n        await walk(root, discovered);\n    const accountDiscovered = [];\n    if (accountRoot) {\n        let accountEntries = [];\n        try {\n            accountEntries = await fs.promises.readdir(accountRoot, { withFileTypes: true });\n        }\n        catch (error) {\n            if (error.code !== \"ENOENT\") {\n                discoveryFailed = true;\n                failures.push(\"A usage directory could not be read.\");\n            }\n        }\n        for (const entry of accountEntries) {\n            if (!entry.isDirectory() && !entry.isSymbolicLink())\n                continue;\n            const files = [];\n            await walk(path.join(accountRoot, entry.name, \"sessions\"), files);\n            await walk(path.join(accountRoot, entry.name, \"archived_sessions\"), files);\n            for (const filePath of files)\n                accountByPath.set(filePath, entry.name);\n            accountDiscovered.push(...files);\n        }\n    }\n    const uniquePaths = [...new Set(discovered)].sort().concat([...new Set(accountDiscovered)].sort());\n    const nextFiles = {};\n    const allRows = new Map();\n    const allEvents = new Map();\n    const seenFiles = new Set();\n    let fileCount = 0;\n    let changedFileCount = 0;\n    let reusedFileCount = 0;\n    for (const filePath of uniquePaths) {\n        const sourceId = crypto.createHash(\"sha256\").update(filePath).digest(\"hex\");\n        const prior = cache.files[sourceId];\n        try {\n            const stat = await fs.promises.stat(filePath);\n            if (stat.mtimeMs < cutoffMs)\n                continue;\n            const fileIdentity = `${stat.dev}:${stat.ino}`;\n            if (seenFiles.has(fileIdentity))\n                continue;\n            seenFiles.add(fileIdentity);\n            fileCount += 1;\n            const signature = `${stat.size}:${Math.trunc(stat.mtimeMs)}`;\n            if (prior?.signature === signature && Array.isArray(prior.rows) && prior.rows.every(validRow)) {\n                nextFiles[sourceId] = prior;\n                for (const row of prior.rows)\n                    row.eventKey ? mergeEvent(allEvents, row) : add(allRows, row);\n                reusedFileCount += 1;\n                continue;\n            }\n            const rows = await parseFile(filePath);\n            nextFiles[sourceId] = { signature, rows };\n            for (const row of rows)\n                row.eventKey ? mergeEvent(allEvents, row) : add(allRows, row);\n            changedFileCount += 1;\n        }\n        catch {\n            failures.push(!canDecompressZstd && filePath.endsWith(\".zstd\")\n                ? \"A Zstandard usage log needs Node.js 22.15+ on this host.\"\n                : \"A usage log could not be read.\");\n            if (prior && Array.isArray(prior.rows) && prior.rows.every(validRow)) {\n                nextFiles[sourceId] = prior;\n                for (const row of prior.rows)\n                    row.eventKey ? mergeEvent(allEvents, row) : add(allRows, row);\n            }\n        }\n    }\n    if (discoveryFailed) {\n        for (const [sourceId, prior] of Object.entries(cache.files)) {\n            if (nextFiles[sourceId] || !Array.isArray(prior.rows) || !prior.rows.every(validRow))\n                continue;\n            nextFiles[sourceId] = prior;\n            for (const row of prior.rows)\n                row.eventKey ? mergeEvent(allEvents, row) : add(allRows, row);\n        }\n    }\n    try {\n        await fs.promises.mkdir(path.dirname(input.cachePath), { recursive: true, mode: 0o700 });\n        const temporaryPath = `${input.cachePath}.${process.pid}.${Date.now()}.tmp`;\n        await fs.promises.writeFile(temporaryPath, JSON.stringify({\n            version: cacheVersion,\n            agentId: input.agentId,\n            files: nextFiles,\n        }), { encoding: \"utf8\", mode: 0o600 });\n        await fs.promises.rename(temporaryPath, input.cachePath);\n    }\n    catch {\n        failures.push(\"The metadata-only usage cache could not be updated.\");\n    }\n    for (const row of allEvents.values())\n        add(allRows, row);\n    const rows = [...allRows.values()].sort((a, b) => a.day.localeCompare(b.day)\n        || a.modelProviderId.localeCompare(b.modelProviderId) || a.model.localeCompare(b.model)\n        || a.project.localeCompare(b.project));\n    const result = {\n        agentId: input.agentId,\n        fileCount,\n        changedFileCount,\n        reusedFileCount,\n        failureCount: failures.length,\n        error: failures[0]?.replace(/[\\r\\n]+/g, \" \").slice(0, 200) ?? null,\n        rows,\n    };\n    const encoded = zlib.gzipSync(JSON.stringify(result)).toString(\"base64\");\n    process.stdout.write(`${scanBegin}\\n${encoded}\\n${scanEnd}\\n`);\n}";

export const devinSqliteCollectorSource = "async function devinSqliteCollector(encodedInput, dependencies) {\n    const { buffer, fs, path, zlib, loadSqlite } = dependencies;\n    const scanBegin = \"__BB_USAGE_SCAN_BEGIN__\";\n    const scanEnd = \"__BB_USAGE_SCAN_END__\";\n    const input = JSON.parse(buffer.from(encodedInput, \"base64\").toString(\"utf8\"));\n    if (input.agentId !== \"devin\")\n        throw new Error(\"Unsupported usage agent.\");\n    if (!/^\\d{4}-\\d{2}-\\d{2}$/.test(input.sinceDay))\n        throw new Error(\"Invalid usage history boundary.\");\n    const result = {\n        agentId: \"devin\",\n        fileCount: 0,\n        changedFileCount: 0,\n        reusedFileCount: 0,\n        failureCount: 0,\n        error: null,\n        rows: [],\n    };\n    const emit = () => {\n        const encoded = zlib.gzipSync(JSON.stringify(result)).toString(\"base64\");\n        process.stdout.write(`${scanBegin}\\n${encoded}\\n${scanEnd}\\n`);\n    };\n    function count(value) {\n        const numeric = typeof value === \"number\" && Number.isFinite(value)\n            ? value\n            : typeof value === \"string\" && value.trim() && Number.isFinite(Number(value)) ? Number(value) : null;\n        return Math.max(0, Math.round(numeric ?? 0));\n    }\n    function text(value, fallback) {\n        return typeof value === \"string\" && value.trim() ? value : fallback;\n    }\n    function projectName(value) {\n        if (typeof value !== \"string\" || !value.trim())\n            return \"Unknown\";\n        const normalized = value.replace(/\\\\/g, \"/\").replace(/\\/+$/, \"\");\n        const segment = normalized.slice(normalized.lastIndexOf(\"/\") + 1);\n        return segment.trim() ? segment.trim().slice(0, 80) : \"Unknown\";\n    }\n    function dayOf(value) {\n        if ((typeof value !== \"string\" && typeof value !== \"number\") || !String(value).trim())\n            return null;\n        const timestamp = new Date(value).getTime();\n        if (!Number.isFinite(timestamp))\n            return null;\n        const parsed = new Date(timestamp);\n        const result = `${parsed.getFullYear()}-${String(parsed.getMonth() + 1).padStart(2, \"0\")}-${String(parsed.getDate()).padStart(2, \"0\")}`;\n        return result >= input.sinceDay ? result : null;\n    }\n    const candidates = [\n        typeof process.env.XDG_DATA_HOME === \"string\" && process.env.XDG_DATA_HOME.trim()\n            ? path.join(process.env.XDG_DATA_HOME.trim(), \"devin/cli/sessions.db\") : \"\",\n        typeof process.env.APPDATA === \"string\" && process.env.APPDATA.trim()\n            ? path.join(process.env.APPDATA.trim(), \"devin/cli/sessions.db\") : \"\",\n        ...input.dbPaths,\n    ].filter(Boolean);\n    const dbPath = candidates.find((candidate) => {\n        try {\n            fs.statSync(candidate);\n            return true;\n        }\n        catch (error) {\n            const code = error.code;\n            if (code === \"ENOENT\" || code === \"ENOTDIR\")\n                return false;\n            throw error;\n        }\n    });\n    if (!dbPath) {\n        emit();\n        return;\n    }\n    const { DatabaseSync } = loadSqlite();\n    const db = new DatabaseSync(dbPath, { readOnly: true });\n    try {\n        const nodes = db.prepare(`SELECT\n        n.session_id sessionId,\n        n.row_id nodeId,\n        n.created_at nodeCreatedAt,\n        s.model model,\n        s.working_directory workingDirectory,\n        json_extract(n.chat_message, '$.metadata.request_id') requestId,\n        json_extract(n.chat_message, '$.metadata.created_at') createdAt,\n        json_extract(n.chat_message, '$.metadata.metrics.input_tokens') inputTokens,\n        json_extract(n.chat_message, '$.metadata.metrics.output_tokens') outputTokens,\n        json_extract(n.chat_message, '$.metadata.metrics.cache_read_tokens') cacheReadTokens,\n        json_extract(n.chat_message, '$.metadata.metrics.cache_creation_tokens') cacheWriteTokens\n      FROM message_nodes n LEFT JOIN sessions s ON s.id = n.session_id\n      WHERE n.chat_message LIKE '%assistant%' AND n.chat_message LIKE '%input_tokens%'\n        AND json_valid(n.chat_message)\n        AND json_extract(n.chat_message, '$.role') = 'assistant'\n        AND json_extract(n.chat_message, '$.metadata.metrics') IS NOT NULL`).all();\n        const events = new Map();\n        for (const node of nodes) {\n            const timestamp = node.createdAt\n                ?? (typeof node.nodeCreatedAt === \"number\" ? node.nodeCreatedAt * 1000 : null);\n            const usageDay = dayOf(timestamp);\n            if (!usageDay)\n                continue;\n            const uncached = count(node.inputTokens);\n            const cached = count(node.cacheReadTokens);\n            const writes = count(node.cacheWriteTokens);\n            const output = count(node.outputTokens);\n            if (uncached + cached + writes + output === 0)\n                continue;\n            const eventKey = typeof node.requestId === \"string\" && node.requestId\n                ? `request:${node.requestId}`\n                : `node:${text(node.sessionId, \"session\")}:${count(node.nodeId)}`;\n            const prior = events.get(eventKey);\n            if (!prior) {\n                events.set(eventKey, {\n                    day: usageDay,\n                    modelProviderId: \"devin\",\n                    model: text(node.model, \"devin-unknown\"),\n                    project: projectName(node.workingDirectory),\n                    loggedCostUsd: null,\n                    uncachedInputTokens: uncached,\n                    cachedInputTokens: cached,\n                    cacheWriteTokens: writes,\n                    outputTokens: output,\n                });\n                continue;\n            }\n            prior.uncachedInputTokens = Math.max(prior.uncachedInputTokens, uncached);\n            prior.cachedInputTokens = Math.max(prior.cachedInputTokens, cached);\n            prior.cacheWriteTokens = Math.max(prior.cacheWriteTokens, writes);\n            prior.outputTokens = Math.max(prior.outputTokens, output);\n        }\n        const aggregates = new Map();\n        for (const event of events.values()) {\n            const key = JSON.stringify([event.day, event.model, event.project]);\n            const prior = aggregates.get(key);\n            if (!prior) {\n                aggregates.set(key, event);\n                continue;\n            }\n            prior.uncachedInputTokens += event.uncachedInputTokens;\n            prior.cachedInputTokens += event.cachedInputTokens;\n            prior.cacheWriteTokens += event.cacheWriteTokens;\n            prior.outputTokens += event.outputTokens;\n        }\n        result.rows = [...aggregates.values()].sort((a, b) => a.day.localeCompare(b.day)\n            || a.modelProviderId.localeCompare(b.modelProviderId) || a.model.localeCompare(b.model)\n            || a.project.localeCompare(b.project));\n        result.fileCount = 1;\n        result.changedFileCount = 1;\n    }\n    finally {\n        db.close();\n    }\n    emit();\n}";

export const normalizeGrokBillingSource = "function normalizeGrokBilling(payload) {\n    const data = payload;\n    if (!data || !Object.prototype.hasOwnProperty.call(data, \"config\"))\n        throw new Error(\"Grok billing response had an unexpected shape.\");\n    if (data.config === null)\n        return [];\n    const c = data.config;\n    if (!c || typeof c !== \"object\" || Array.isArray(c))\n        throw new Error(\"Grok billing response had an unexpected shape.\");\n    const cents = (value) => {\n        if (value == null)\n            return null;\n        if (typeof value !== \"object\" || Array.isArray(value))\n            throw new Error(\"Grok billing response contained invalid credits.\");\n        const n = value.val === undefined ? 0 : typeof value.val === \"string\" && /^\\d+$/.test(value.val) ? Number(value.val) : value.val;\n        if (typeof n !== \"number\" || !Number.isSafeInteger(n) || n < 0)\n            throw new Error(\"Grok billing response contained invalid credits.\");\n        return n;\n    };\n    const date = (value) => typeof value === \"string\" && Number.isFinite(Date.parse(value)) ? new Date(value).toISOString() : null;\n    const limit = cents(c.monthlyLimit);\n    const used = cents(c.used) ?? 0;\n    const percent = c.creditUsagePercent ?? (limit && limit > 0 ? used / limit * 100 : null);\n    const windows = [];\n    if (percent !== null) {\n        if (typeof percent !== \"number\" || !Number.isFinite(percent) || percent < 0)\n            throw new Error(\"Grok billing response contained an invalid usage percentage.\");\n        const period = c.currentPeriod?.type;\n        const label = period === \"USAGE_PERIOD_TYPE_WEEKLY\" ? \"Weekly\" : period === \"USAGE_PERIOD_TYPE_MONTHLY\" || (!c.currentPeriod && limit !== null) ? \"Monthly\" : \"Current period\";\n        windows.push({ label: `${label}${c.isUnifiedBillingUser === true ? \" (shared credits)\" : \" credits\"}`, usedPercent: Math.min(100, percent), resetsAt: date(c.currentPeriod?.end ?? c.billingPeriodEnd) });\n    }\n    const cap = cents(c.onDemandCap);\n    if (cap !== null && cap > 0) {\n        const spent = cents(c.onDemandUsed) ?? Math.max(0, used - (limit ?? 0));\n        windows.push({ label: \"On-demand\", usedPercent: Math.min(100, spent / cap * 100), resetsAt: date(c.billingPeriodEnd), cost: { usedUsdCents: spent, limitUsdCents: cap } });\n    }\n    if (!windows.length)\n        throw new Error(\"Grok billing response contained no limit windows.\");\n    return windows;\n}";

